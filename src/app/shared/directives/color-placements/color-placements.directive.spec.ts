import { ColorPlacementsDirective } from './color-placements.directive';

describe('ColorPlacementsDirective', () => {
  it('should create an instance', () => {
    const directive = new ColorPlacementsDirective();
    expect(directive).toBeTruthy();
  });
});
