import { EditLayerDirective } from './edit-layer.directive';

describe('EditLayerDirective', () => {
  it('should create an instance', () => {
    const directive = new EditLayerDirective();
    expect(directive).toBeTruthy();
  });
});
